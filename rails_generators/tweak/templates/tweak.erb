Tweaks.define_tweak(
  :<%= args.first %>,	# name of tweak
  :disabled, # determines whether tweak is normally :enabled or :disabled
  {
    # default config values here
  }
) do |config,name|

  # The tweak does its thing here
  # Of course, you can declare a class or whatever above to just call here with the config

end

